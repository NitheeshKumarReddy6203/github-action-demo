
name: Context Variables

on:

  workflow_dispatch:

  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  firstName: 'Nitheesh'

jobs:
  context-varibales:
    runs-on: ubuntu-latest
    env:
      middleName: 'Kumar'
    steps:
      - uses: actions/checkout@v4
      - name: Intro
        run: |
          echo "Hello Everyone"
          echo "Today we are here to Practice the Context Variables"

      - name: Getting the Context Variables
        run: |
          echo "My Repository Name: ${{ github.repository }} !"
          echo "My Workflow Name: ${{ github.workflow }} !"
          echo "My Triggred Action Name: ${{ github.triggering_actor }} !"

        # TEST: test123, TEST1 : echo Hello
      - name: Getting a Secret Value from Repository
        run: |
          
          echo "Secret value for test is: ${{ secrets.TEST }} "
          echo " "
          echo "Secret Value for test 2 is : ${{ secrets.TEST1 }} "
          echo "-*-"